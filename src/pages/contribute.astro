---
import BaseLayout from '../layouts/BaseLayout.astro';

const lang = 'en';
---

<BaseLayout title="Write With Us | Ayamatma" description="The template every article must complete before writing begins." lang={lang}>
  <section class="section stack">
    <h1 class="section-title">Write With Us</h1>
    <p class="muted">Before you write, answer these.</p>
  </section>

  <section class="section">
    <div class="template-list">
      <div class="template-item">
        <span class="item-num">1</span>
        <div class="item-content">
          <h3>Title</h3>
          <p>Clear. Descriptive. Not clickbait.</p>
        </div>
      </div>

      <div class="template-item">
        <span class="item-num">2</span>
        <div class="item-content">
          <h3>Sanskrit Terms</h3>
          <p>Which 3-5 terms will you define with precision?</p>
        </div>
      </div>

      <div class="template-item">
        <span class="item-num">3</span>
        <div class="item-content">
          <h3>The Fallacy</h3>
          <p>What misunderstanding are you correcting? If none, why write?</p>
        </div>
      </div>

      <div class="template-item">
        <span class="item-num">4</span>
        <div class="item-content">
          <h3>The Steel Man</h3>
          <p>State the opposing view so well its holder would agree.</p>
        </div>
      </div>

      <div class="template-item">
        <span class="item-num">5</span>
        <div class="item-content">
          <h3>The Practice</h3>
          <p>How does this change how someone sits, breathes, or sees?</p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="closing">
      <p>If any answer is blank, the article is not ready.</p>
      <p>This applies to all contributors—including us.</p>
    </div>
  </section>

  <section class="section">
    <div class="cta-block">
      <a href="/essays" class="cta-link">
        <span class="cta-label">Enter</span>
        <span class="cta-desc">The joys of knowing</span>
      </a>
    </div>
  </section>
</BaseLayout>

<style>
  .template-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .template-item {
    display: flex;
    gap: 1.25rem;
    align-items: flex-start;
    padding: 1.25rem 1.5rem;
    background: var(--bg-card);
    backdrop-filter: blur(12px);
    border: 1px solid var(--glass-border);
    border-radius: 12px;
  }

  .item-num {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.5rem;
    font-weight: 300;
    color: var(--accent-warm);
    opacity: 0.7;
    line-height: 1;
    min-width: 1.5rem;
  }

  .item-content h3 {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--text);
    margin: 0 0 0.25rem 0;
  }

  .item-content p {
    font-size: 0.95rem;
    color: var(--muted);
    margin: 0;
    line-height: 1.5;
  }

  .closing {
    text-align: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .closing p {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.05rem;
    color: var(--text-soft);
    margin-bottom: 0.5rem;
    line-height: 1.6;
  }

  .closing p:last-child {
    font-style: italic;
    color: var(--muted);
  }

  .cta-block {
    text-align: center;
    padding: 1rem 0;
  }

  .cta-link {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    padding: 1.25rem 2.5rem;
    background: linear-gradient(135deg, var(--accent-cool) 0%, var(--accent-warm) 100%);
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .cta-link:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }

  .cta-label {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.15rem;
    font-weight: 500;
    color: white;
    margin-bottom: 0.25rem;
  }

  .cta-desc {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.85);
  }
</style>
