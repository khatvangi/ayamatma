---
import ThemeToggle from './ThemeToggle.astro';
import LanguageSwitcher from './LanguageSwitcher.astro';

const { lang = 'en' } = Astro.props;
const base = lang === 'en' ? '' : `/${lang}`;
const nav = [
  { label: 'Primer', href: `${base}/primer` },
  { label: 'Essays', href: `${base}/essays` },
  { label: 'Dictionary', href: `${base}/dictionary` },
  { label: 'Protocols', href: `${base}/manifesto` },
  { label: 'Vāda', href: `${base}/vada` },
];
---

<header class="container">
  <nav class="nav" aria-label="Primary">
    <a class="nav-brand" href={`${base}/`}>
      <img src="/images/logo-circle-sm.webp" alt="ayamatma" class="nav-logo" width="128" height="128" />
    </a>
    <div class="nav-links">
      {nav.map((item) => (
        <a href={item.href}>{item.label}</a>
      ))}
      <LanguageSwitcher lang={lang} />
      <ThemeToggle />
    </div>
  </nav>
</header>
