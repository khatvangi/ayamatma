---
// splash screen - the gate to enter
---

<div class="splash-overlay" id="splash-overlay">
  <div class="splash-gate">
    <p class="gate-sutra">जिज्ञासायाम् आनन्दः</p>
    <p class="gate-translit">Jijñāsāyām ānandaḥ</p>
    <p class="gate-meaning">In jijñāsā, there is ānanda.</p>
    <button class="gate-enter">
      <span class="enter-label">Enter</span>
      <span class="enter-desc">Into the joys of knowing</span>
    </button>
  </div>
</div>

<style is:global>
  .splash-overlay {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    width: 100vw !important;
    height: 100vh !important;
    z-index: 99999 !important;
    background: var(--bg-start, #fafaf8);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    overflow: hidden;
  }

  .splash-overlay.melting {
    animation: melt 600ms ease-out forwards;
  }

  @keyframes melt {
    0% {
      opacity: 1;
      transform: scale(1);
    }
    100% {
      opacity: 0;
      transform: scale(1.02);
      pointer-events: none;
    }
  }

  .splash-gate {
    text-align: center;
    max-width: 500px;
  }

  .gate-sutra {
    font-family: 'Fraunces', Georgia, serif;
    font-size: clamp(2.2rem, 10vw, 3.5rem);
    font-weight: 400;
    color: var(--accent-warm, #c96442);
    margin: 0 0 1rem 0;
    line-height: 1.3;
  }

  .gate-translit {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.35rem;
    font-style: italic;
    color: var(--text, #2d3436);
    margin: 0 0 0.4rem 0;
  }

  .gate-meaning {
    font-family: 'DM Sans', sans-serif;
    font-size: 1.05rem;
    color: var(--muted, #718096);
    margin: 0 0 2.5rem 0;
  }

  .gate-enter {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    padding: 1.25rem 2.5rem;
    background: linear-gradient(135deg, var(--accent-cool, #2d7a6f) 0%, var(--accent-warm, #c96442) 100%);
    border: none;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .gate-enter:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }

  .enter-label {
    font-family: 'Fraunces', Georgia, serif;
    font-size: 1.15rem;
    font-weight: 500;
    color: white;
    margin-bottom: 0.25rem;
  }

  .enter-desc {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.85);
  }

  body:has(.splash-overlay:not(.melting)) {
    overflow: hidden;
  }

  @media (max-width: 500px) {
    .gate-translit {
      font-size: 1.15rem;
    }

    .gate-meaning {
      font-size: 0.95rem;
    }
  }
</style>

<script is:inline>
  (function() {
    var splash = document.getElementById('splash-overlay');
    if (!splash) return;

    var btn = splash.querySelector('.gate-enter');
    if (btn) {
      btn.addEventListener('click', function() {
        splash.classList.add('melting');
        setTimeout(function() {
          splash.remove();
        }, 600);
      });
    }
  })();
</script>
